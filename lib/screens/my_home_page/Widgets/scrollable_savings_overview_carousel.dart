import 'package:flutter/material.dart';

class SavingsCarousel extends StatelessWidget {
  const SavingsCarousel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
